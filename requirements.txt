# ZLink Agent dependencies — single source of truth is pyproject.toml
# This file is kept for setup.sh compatibility and CI.
# Install the project in editable mode to pull all deps from pyproject.toml:
#
#   pip install -e .
#
-e .
