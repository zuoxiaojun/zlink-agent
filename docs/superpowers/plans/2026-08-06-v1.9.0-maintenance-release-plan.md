# v1.9.0 维护发布 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把上一会话 Pi 风格内核重写（merge `1671a13`）遗留的维护工作收进 v1.9.0 发布：A 组 6 个 parked minors（M-1/M-2/M-3/M-5/M-6/M-7，每个配测试）、B 组 `TAVILY_API_KEY` 运行时 `.env` 加载（不打包）、C 组 4 项前端优化、D 组版本四件套 + tag 发布。D 是最后一步，把 A/B/C 全部改动统一收进 v1.9.0。

**Architecture:** A 组全部是逐层透传的小改动：M-1 在 `ToolExecutionEnd` 加可选 `denied` 字段（默认 `False`，既有构造点零改动）一路透传到前端卡片"已拒绝"状态；M-2 在取消分支把 `_stream_cb` 累积的 `_partial_response` 补进持久化消息；M-5 补两处 `_set_phase` 的 `session_id`；M-6 改 `Agent.run_async` token 采纳与 `subscribe` 幂等；M-7 纯删死代码。B 组把 `backend/config.py` 的 `load_dotenv` 改为候选列表加载（`~/.zlink-agent/.env` 优先，`override=False`）。C 组纯 CSS 类 + JSX 类名替换，零视觉变化（C-1 布局放宽除外）。D 组按 `AGENTS.md §10` 一次性 bump 版本四件套 + tag。

**Tech Stack:** Python 3.11+ / asyncio / FastAPI WebSocket / pytest（零网络）/ ruff；React + Vite + TypeScript / @tabler/icons-react / CSS variables。全程不引入新依赖。

## Global Constraints（spec §8 决策 + §7 风险缓解，所有任务隐式包含）

- **零网络测试策略**：所有后端测试用 `MockLLMProvider` + tmp 配置隔离；不发起真实 LLM/ERP/MCP 调用；测试中的压缩 summary 走 `MockLLMProvider` 脚本化响应。
- **四个冻结契约（spec §6 验收标准 5，任何任务不得破坏）：**
  - C1 `run_conversation` / `run_conversation_async` 返回 dict 6 keys 与语义不变（`{final_response, messages, api_calls, token_usage, completed, error}`）；`AIAgent` 构造签名不变。
  - C2 WS 消息集合：**仅允许** `tool_result` 新增**可选字段** `denied`（缺省即旧行为）；其余 `type` 与字段逐字不变。
  - C3 EventBus 8 事件类型/字段/取消语义不变；`agent/events/` 零改动。
  - C4 registry 公开接口（`register`/`deregister`/`dispatch`/`get_definitions`/`get_all_tool_names`/hook 增删/block 协议）不变；`registry.py`、`security_hooks.py` 零改动。
- **Do-Not-Modify（AGENTS.md §5）**：`agent/skills/`、`backend/schemas/`、`agent/tools/registry.py`、`agent/tools/security_hooks.py`、`agent/events/`、`agent/erp_clients/`、`skill_manager`、`memory_manager`、`fact_memory`、`search_index`、`message_builder.py` 一律不动。
- **不引入新依赖**：后端仅标准库 + 既有 `python-dotenv`；前端不新增 npm 包。
- **M-1 决策**：hook 拦截（block/immediate）路径保持"不下发卡片"现状不变，本项只覆盖**审批拒绝**路径；`ToolResult`（transcript 错误 JSON，`is_error=True`）语义不变。
- **M-2 决策**：只把 `_partial_response` 补进取消分支的 `_result_messages`（持久化数据）；**不改** `_final_response`（保持空）→ WS `done` 与前端 UI 逐字不变；工具执行中取消的其余 transcript 丢失为已知局限，不扩大改动。
- **B 组决策**：key 写入 `~/.zlink-agent/.env`（`DATA_DIR.parent`，用户数据目录，**不打包进 PyInstaller**）；`load_dotenv(override=False)` 使环境变量 > 先加载文件 > 后加载文件；`scripts/build-pyinstaller.sh`、`electron/main.js`、`electron/backend_launcher.py` **零改动**；dev 模式沿用仓库根 `.env` 现状。
- **C-2 原则（spec §4 C-2）**：纯静态 inline style → CSS 类，**逐字复制属性值（含 `var(--bg-2)`/`var(--bg)`/`var(--error)` 等既有未定义引用，保持当前计算行为）**，不改变任何视觉与交互；动态样式（依赖 state/运行时值）保持 inline；每批门禁 `npm run build` + `npm run lint` 全绿。
- **前端验证**：无前端测试框架——C 组每批与 M-1/M-3 的前端改动验证 = `npm run build` + `npm run lint` + 手工走查（spec §5.2 清单）。
- **版本唯一事实源**：`pyproject.toml` 为版本唯一事实源；根 `package.json` 必须同步（electron-builder 用其命名 DMG/App）；D 之前所有改动**只提交不 bump 版本**。
- **提交规则**：`docs/` 在 `.gitignore` 中——本计划文件本身用 `git add -f docs/superpowers/plans/2026-08-06-v1.9.0-maintenance-release-plan.md` 提交；代码文件用普通 `git add`。commit message 一律英文（仓库约定）；文档/用户可见字符串保持中文现状。
- **测试命令**：后端单测 `.venv/bin/python -m pytest tests/<file>.py -v`；全量 `.venv/bin/python -m pytest tests/ -q`；lint `ruff check .`。前端 `cd web && npm run build && npm run lint`（必须在 web 目录下执行）。
- **spec 明确不做（非目标）**：M-4（opencode 全局配置）、前端候选 5、旧内核回退（`ZLINK_KERNEL` 已删除）、前端测试框架、后端鉴权、C-3 的 `#fff` 白字类硬编码（ChatMessage.tsx:114、SettingsERPPage:228/244、SettingsExtensionsPage:144 属"彩色底白字"，保持现状）。

---

## File Structure（文件规划）

| 路径 | 动作 | 职责 |
|------|------|------|
| `agent/core/kernel_types.py` | 修改 | `ToolExecutionEnd` 新增可选字段 `denied: bool = False` |
| `agent/core/tool_dispatcher.py` | 修改 | 新增内部 `_HandlerResult`；`_run_handler` 返回类型改造；`_dispatch_parallel`/`_dispatch_sequential` 透传 `denied`；删除 `dispatch_tool` 死代码 + docstring + `__all__` |
| `agent/core/__init__.py` | 修改 | 删除 `dispatch_tool` re-export |
| `agent/core/agent_adapter.py` | 修改 | `_partial_response` 累积与取消分支追加；`_transform_context_hook` 两处 `_set_phase` 补 `session_id` |
| `agent/core/agent.py` | 修改 | `run_async` token 采纳逻辑；`subscribe` 幂等化 |
| `backend/api/chat.py` | 修改 | `tool_execution_end` 分支给 `tool_result` 附加可选 `denied` |
| `backend/config.py` | 修改 | `load_dotenv` → `_load_env_files()` 候选列表 |
| `tests/test_kernel_types.py` | 修改 | `ToolExecutionEnd.denied` 默认断言 |
| `tests/test_tool_dispatcher_batch.py` | 修改 | denied/approved 两路 `ToolExecutionEnd.denied` 断言 |
| `tests/test_agent_adapter.py` | 修改 | 新增 M-2 取消保留 partial、M-5 压缩 session_id 用例 |
| `tests/test_agent.py` | 修改 | M-6：新 token/无 token 复用、subscribe 幂等；更新 `test_cancel_and_wait_idle` |
| `tests/test_backend_config_env.py` | 新建 | `_load_env_files()` 候选加载 + override=False 语义 |
| `web/src/types/index.ts` | 修改 | `WsServerMessage.tool_result.denied?: boolean`；`Message._denied?: boolean` |
| `web/src/hooks/useChat.ts` | 修改 | `tool_result` 分支透传 `denied` |
| `web/src/context/AppContext.tsx` | 修改 | `REPLACE_PENDING_TOOL` action 增加 `denied?: boolean`；reducer 置 `_denied` |
| `web/src/components/ToolStepCard.tsx` | 修改 | `denied` 状态判定（优先级 denied > failed）；`effectiveArgs` 回退链；`extractSubtitle` 桥工具 case |
| `web/src/styles/global.css` | 修改 | `.tool-step-denied`、`.badge-neutral`、`.page-container` 1280px、`.card-grid`、`.form-grid-2`、`.test-result-ok/error`、C-2 工具类 |
| `web/src/pages/HistoryPage.tsx` | 修改 | C-1 卡片网格；C-4 中性灰 hash 徽章 + 删除 confirm |
| `web/src/pages/MemoryPage.tsx` / `ToolsPage.tsx` | 修改 | C-1 卡片容器 → `card-grid`；C-2 内联样式收敛 |
| `web/src/pages/SettingsLLMPage.tsx` / `SettingsAgentPage.tsx` / `SettingsERPPage.tsx` | 修改 | C-1 `.form-grid-2`；C-2 内联样式收敛 |
| `web/src/pages/McpPage.tsx` / `SkillManagerPage.tsx` / `SettingsExtensionsPage.tsx` / `CronJobPage.tsx` / `Skeleton.tsx` / `Layout.tsx` / `SettingsLLMPage.tsx` / `Sidebar.tsx` / `ChatMessage.tsx` / `ChatInput.tsx` | 修改 | C-2 内联样式收敛 |
| `pyproject.toml` / `package.json` / `CHANGELOG.md` / `README.md` | 修改 | D 组版本四件套 1.8.1 → 1.9.0 |

---

# A 组 —— parked minors 后端清理（每个配测试）

## Task 1: M-1 —— 审批拒绝的 `ToolExecutionEnd.denied` 全链路透传（后端 + 前端）

**Files:**
- Modify: `agent/core/kernel_types.py:187-193`（`ToolExecutionEnd`）
- Modify: `agent/core/tool_dispatcher.py`（新增 `_HandlerResult`；`_run_handler` 返回类型；`_dispatch_parallel` 的 `_run`/`by_index`/`ToolExecutionEnd` 构造；`_dispatch_sequential` 的 `ToolExecutionEnd` 构造）
- Modify: `backend/api/chat.py:404-407`（`tool_execution_end` 分支）
- Modify: `web/src/types/index.ts:196`；`web/src/hooks/useChat.ts:82-84`；`web/src/context/AppContext.tsx:63,141`；`web/src/components/ToolStepCard.tsx:104-160`；`web/src/styles/global.css`（`.tool-step-denied`）
- Test: `tests/test_kernel_types.py`（TestAgentEvents）、`tests/test_tool_dispatcher_batch.py:251-320`

**Interfaces:**
- Consumes: 现有 `ToolExecutionEnd(tool_call_id, tool_name, result, is_error=False, type=...)`（`agent/core/kernel_types.py`）；`tool_error()`（`agent.tools.registry`）；WS `tool_result` 消息（`backend/api/chat.py` 现有扁平协议）；`REPLACE_PENDING_TOOL` action（`AppContext.tsx`）。
- Produces（后续任务与前端依赖，签名冻结）：
  - `@dataclass(frozen=True) ToolExecutionEnd` 新增字段 `denied: bool = False`（置于 `is_error` 之后、`type` 之前——`type` 判别式字段保持末尾）
  - `@dataclass class _HandlerResult`（tool_dispatcher 内部）：`result: str`；`denied: bool = False`
  - `_run_handler(name, args, config, token) -> _HandlerResult`（返回值从 `str` 改为 `_HandlerResult`，调用方统一取 `.result`）
  - WS 消息：`{"type": "tool_result", "name": <str>, "result": <str>[, "denied": true]}`——`denied` 仅在 `event.denied` 为真时附加
  - 前端：`WsServerMessage.tool_result` 增加 `denied?: boolean`；`Message` 增加 `_denied?: boolean`；`REPLACE_PENDING_TOOL` action 增加 `denied?: boolean`

- [ ] **Step 1: 写失败测试**

1a. 在 `tests/test_kernel_types.py` 的 `TestAgentEvents.test_discriminator_types` 末尾（`assert ToolExecutionEnd(tool_call_id="c1", tool_name="ls", result="err", is_error=True).is_error is True` 之后）追加一行：

```python
        assert ToolExecutionEnd(tool_call_id="c1", tool_name="ls", result="err", is_error=True).denied is False
```

1b. 修改 `tests/test_tool_dispatcher_batch.py` 两个审批用例（行 251-320），把内联 `emit=_EventRecorder()` 改为具名 recorder 并追加断言：

`test_approval_blocked_denied_returns_denial`（现 251-284 行）——把 `batch = _run(` 前的 `_run(\n            dispatch_tool_batch(` 调用改为：

```python
        recorder = _EventRecorder()
        batch = _run(
            dispatch_tool_batch(
                [ToolCallPayload(id="c1", name="t_approve", arguments="{}")],
                max_result_length=sys.maxsize,
                token=CancelToken(),
                emit=recorder,
                config=_make_config(on_approval_blocked=_on_blocked),
            )
        )
    finally:
        registry.remove_before_hook(_raise_hook)

    assert decisions == ["t_approve"]
    assert batch.messages[0].is_error is True
    assert "用户拒绝了操作" in batch.messages[0].result
    starts = [e for e in recorder.events if isinstance(e, ToolExecutionStart)]
    ends = [e for e in recorder.events if isinstance(e, ToolExecutionEnd)]
    assert len(starts) == 1, "denial path must emit exactly one ToolExecutionStart"
    assert len(ends) == 1
    assert ends[0].denied is True
    assert ends[0].is_error is True
```

`test_approval_blocked_approved_runs_handler_directly`（现 287-320 行）——把 `emit=_EventRecorder()` 改为 `recorder = _EventRecorder()`（同上的具名形式），并在末尾追加：

```python
    assert ran == ["handler"]
    assert json.loads(batch.messages[0].result)["data"]["approved"] is True
    ends = [e for e in recorder.events if isinstance(e, ToolExecutionEnd)]
    assert len(ends) == 1
    assert ends[0].denied is False
```

同时给该用例的注册调用追加 `execution_mode="sequential"`（覆盖 sequential 路径的 `denied=False` 透传；denied=True 由前一个用例覆盖 parallel 路径）：

```python
registry.register(
    name="t_approve2",
    toolset="test",
    schema={"type": "object"},
    handler=handler,
    risk_level="high",
    execution_mode="sequential",
)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_kernel_types.py tests/test_tool_dispatcher_batch.py -v`

Expected: FAIL —— `AttributeError: 'ToolExecutionEnd' object has no attribute 'denied'`（kernel_types 断言处）。

- [ ] **Step 3: 最小实现——kernel_types 加字段**

在 `agent/core/kernel_types.py` 的 `ToolExecutionEnd`（187-193 行）中，`is_error: bool = False` 之后、`type: str = "tool_execution_end"` 之前插入一行：

```python
    denied: bool = False
```

（`dataclass(frozen=True)` 带默认值，所有现有构造点零改动。）

- [ ] **Step 4: 最小实现——tool_dispatcher 透传**

4a. 在 `_Prepared` dataclass（98-103 行）之后新增：

```python
@dataclass
class _HandlerResult:
    result: str
    denied: bool = False
```

4b. `_run_handler`（276-307 行）返回类型改为 `_HandlerResult`，denial 分支标记 `denied=True`，其余路径 `denied=False`。整段替换为：

```python
async def _run_handler(
    name: str,
    args: dict,
    config: AgentLoopConfig,
    token: CancelToken,
) -> _HandlerResult:
    """Execute one tool.

    Bridge tools go through ``config.bridge_dispatch`` (progressive
    disclosure); everything else runs through ``registry.dispatch`` in a
    worker thread so the security hook chain and the ``__block__``
    protocol are preserved (Layer 2).  ApprovalBlockedError is routed to
    ``config.on_approval_blocked``; an approved retry calls the raw
    handler directly (mirrors the legacy approved path).  A denial is
    flagged via ``_HandlerResult.denied`` so the frontend can show
    "已拒绝" instead of a generic failure.
    """
    if config.bridge_dispatch is not None and name in BRIDGE_TOOL_NAMES:
        raw = await _await_maybe(config.bridge_dispatch(name, args))
        return _HandlerResult(raw if isinstance(raw, str) else json.dumps(raw, ensure_ascii=False))
    try:
        raw = await asyncio.to_thread(registry.dispatch, name, args)
    except Exception as e:  # noqa: BLE001
        if type(e).__name__ == "ApprovalBlockedError":
            decision = "denied"
            if config.on_approval_blocked is not None:
                decision = await _await_maybe(config.on_approval_blocked(name, str(e)))
            if decision == "approved":
                raw = await asyncio.to_thread(_dispatch_bypassing_hooks, name, args)
                return _HandlerResult(raw if isinstance(raw, str) else json.dumps(raw, ensure_ascii=False))
            return _HandlerResult(tool_error("用户拒绝了操作"), denied=True)
        logger.exception("Tool %s failed", name)
        return _HandlerResult(tool_error(str(e)))
    return _HandlerResult(raw if isinstance(raw, str) else json.dumps(raw, ensure_ascii=False))
```

4c. `_dispatch_parallel`（138-199 行）内 `_run`（158-161 行）改为四元组，`by_index`（167 行）存四元组，`ToolExecutionEnd` 构造（183-190 行）透传 `denied`：

```python
async def _run(prep: _Prepared) -> tuple[int, str, bool, bool]:
    res = await _run_handler(prep.tc.name, prep.args, config, token)
    finalized = await _finalize(prep.tc.name, prep.args, res.result, config, token)
    return prep.index, _truncate(finalized, max_result_length), _is_error_result(finalized), res.denied


if runnable:
    completed = await asyncio.gather(*(_run(p) for p in runnable))
else:
    completed = []
by_index = {idx: (truncated, is_error, denied) for idx, truncated, is_error, denied in completed}
```

```python
        truncated, is_error, denied = by_index[p.index]
        await emit(
            ToolExecutionEnd(
                tool_call_id=p.tc.id,
                tool_name=p.tc.name,
                result=truncated,
                is_error=is_error,
                denied=denied,
            )
        )
```

4d. `_dispatch_sequential`（202-246 行）的 handler 调用与 `ToolExecutionEnd` 构造（226-237 行）改为：

```python
        await emit(ToolExecutionStart(tool_call_id=tc.id, tool_name=tc.name, args=args))
        raw = await _run_handler(tc.name, args, config, token)
        finalized = await _finalize(tc.name, args, raw.result, config, token)
        truncated = _truncate(finalized, max_result_length)
        is_error = _is_error_result(finalized)
        await emit(
            ToolExecutionEnd(
                tool_call_id=tc.id,
                tool_name=tc.name,
                result=truncated,
                is_error=is_error,
                denied=raw.denied,
            )
        )
```

- [ ] **Step 5: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_kernel_types.py tests/test_tool_dispatcher_batch.py -v`

Expected: PASS（`test_kernel_types.py` 13 passed；`test_tool_dispatcher_batch.py` 14 passed）。再跑一次涉及 `ToolExecutionEnd` 构造的存量回归：

Run: `.venv/bin/python -m pytest tests/test_loop.py tests/test_agent.py -q`

Expected: PASS，`0 failed`。

- [ ] **Step 6: 后端 commit**

```bash
git add agent/core/kernel_types.py agent/core/tool_dispatcher.py tests/test_kernel_types.py tests/test_tool_dispatcher_batch.py
git commit -m "feat: mark denied tool executions via ToolExecutionEnd.denied"
```

- [ ] **Step 7: chat.py 透传 `denied` 到 WS**

`backend/api/chat.py:404-407` 替换为：

```python
        elif t == "tool_execution_end":
            payload: dict = {"type": "tool_result", "name": event.tool_name, "result": event.result}
            if event.denied:
                payload["denied"] = True
            send_tasks.append(asyncio.create_task(_send(payload)))
```

- [ ] **Step 8: 前端 5 处透传**

8a. `web/src/types/index.ts:196`：

```ts
  | { type: "tool_result"; name: string; result: string; denied?: boolean }
```

8b. `web/src/types/index.ts:22`（`Message` 接口内）：

```ts
  _denied?: boolean;     // 前端临时标记：工具调用被用户拒绝
```

8c. `web/src/hooks/useChat.ts:82-84`：

```ts
          case "tool_result":
            dispatch({ type: "REPLACE_PENDING_TOOL", name: msg.name, result: msg.result, denied: msg.denied });
            break;
```

8d. `web/src/context/AppContext.tsx:63`：

```ts
  | { type: "REPLACE_PENDING_TOOL"; name: string; result: string; denied?: boolean }
```

8e. `web/src/context/AppContext.tsx:141`：

```ts
      msgs[targetIdx] = { ...msgs[targetIdx], content: action.result, _tool_done: true, ...(action.denied ? { _denied: true } : {}) };
```

- [ ] **Step 9: ToolStepCard 状态判定（denied > failed）+ 图标 + 文案**

`web/src/components/ToolStepCard.tsx`：

9a. import 行（2-9 行）`IconAlertCircle,` 后加 `IconAlertTriangle,`（该图标已在本仓库 ApprovalCard.tsx 使用，确认可用）：

```ts
import {
  IconChevronDown,
  IconChevronRight,
  IconCircleCheck,
  IconAlertCircle,
  IconAlertTriangle,
  IconPlayerPlay,
  IconClock,
} from "@tabler/icons-react";
```

9b. 116 行（`running`/`failed` 计算）替换为：

```ts
  const running = !result || isPendingTool;
  const denied = result?._denied === true;
  const failed = !running && !denied && raw.trim() ? isErrorResult(raw) : false;
```

9c. 150 行 className 追加 `tool-step-denied`：

```tsx
    <div className={`tool-step${running ? " tool-step-running" : ""}${denied ? " tool-step-denied" : ""}${failed ? " tool-step-error" : ""}`}>
```

9d. 154 行图标（优先级 running > denied > failed）：

```tsx
            {running ? <div className="tool-step-spinner" /> : denied ? <IconAlertTriangle size={16} /> : failed ? <IconAlertCircle size={16} /> : <IconCircleCheck size={16} />}
```

9e. 160 行文案：

```tsx
            <div className="tool-step-desc">{running ? "正在执行…" : denied ? "已拒绝" : failed ? "执行失败" : "执行完成"}</div>
```

- [ ] **Step 10: global.css 新增 `.tool-step-denied`**

在 `web/src/styles/global.css` 的 `.tool-step-error .tool-step-desc` 规则（662-664 行）之后插入：

```css
.tool-step-denied {
  border-color: var(--warning);
}
.tool-step-denied .tool-step-icon {
  background: var(--warning-bg);
  color: var(--warning);
}
.tool-step-denied .tool-step-desc {
  color: var(--warning);
}
```

（`--warning: #FF7D00` / `--warning-bg: #FFF7E8` 已在 `:root` 定义，与 `.tool-step-error` 使用 `--danger` 系并列。）

- [ ] **Step 11: 前端门禁验证**

Run: `cd web && npm run build && npm run lint`

Expected: `npm run build` 无 TS 错误（`tsc -b && vite build` 成功，生成 `dist/`）；`npm run lint` 输出 `0 errors` / 无错误退出。

- [ ] **Step 12: 全量回归 + 前端 commit**

Run: `cd .. && .venv/bin/python -m pytest tests/ -q && ruff check .`

Expected: `>= 501 passed`（原 501 + 本任务断言扩展，无新增用例），`0 failed`；`ruff check .` 无输出。

```bash
git add backend/api/chat.py web/src/types/index.ts web/src/hooks/useChat.ts web/src/context/AppContext.tsx web/src/components/ToolStepCard.tsx web/src/styles/global.css
git commit -m "feat: show denied state on tool cards via WS tool_result.denied"
```

---

## Task 2: M-2 —— 取消时会话持久化补全流式内容

**Files:**
- Modify: `agent/core/agent_adapter.py`（`__init__` 初始化 `_partial_response`；`run_conversation_async` 重置；`_call_llm_hook` 的 `_stream_cb` 累积；`except asyncio.CancelledError` 分支追加）
- Test: `tests/test_agent_adapter.py`（新增用例）

**Interfaces:**
- Consumes: `AIAgent._agent.state.messages`（`Agent.state`，`agent/core/agent.py`）；`run_conversation_async(..., stream_callback, session_id)` 现有签名（`agent_adapter.py:515-524`）；`tests/conftest.py` 的 `MockLLMProvider`/`make_tool_call_response`。
- Produces: 取消路径返回 dict 的 `messages` 可能追加一条 `{"role": "assistant", "content": <已流式片段>}`；`final_response` 保持 `""`；`completed` 保持 `False`（`error` 仍为 `"用户已手动停止"`）。`backend/api/chat.py` 持久化逻辑零改动（`result["messages"][len(history)+1:]` 自然切出该消息）。

- [ ] **Step 1: 写失败测试**

在 `tests/test_agent_adapter.py` 的 `TestNewKernelPath` 类中（`test_cancel_during_run_ends_with_agent_end` 用例之后，约 380 行处）新增：

```python
    def test_cancel_keeps_partial_streamed_content(self, monkeypatch):
        import time

        def _sleepy(args: dict) -> str:
            time.sleep(0.5)
            return json.dumps({"success": True})

        registry.register(name="partial_sleepy", toolset="test", schema={"type": "object"}, handler=_sleepy)

        class _StreamingProvider:
            """First call streams chunks through stream_callback then returns a
            tool-call response (content ""); the sleepy tool then blocks the run
            so the test can cancel mid-run while _partial_response is populated."""

            def __init__(self, inner):
                self.inner = inner
                self.first = True

            def chat(self, **kwargs):
                stream_cb = kwargs.get("stream_callback")
                if self.first:
                    self.first = False
                    if stream_cb:
                        stream_cb("streamed-")
                        stream_cb("partial")
                    return make_tool_call_response("partial_sleepy", {})
                return self.inner.chat(**kwargs)

        agent = AIAgent(api_key="sk-fake", base_url="x", model="gpt-4o", max_iterations=3)
        agent._llm = LLMClient(
            api_key="sk-fake",
            base_url="x",
            provider=_StreamingProvider(MockLLMProvider()),
        )

        async def _scenario() -> dict:
            task = asyncio.create_task(
                agent.run_conversation_async(
                    user_message="go",
                    conversation_history=[],
                    session_id="s1",
                    stream_callback=lambda t: None,
                )
            )
            await asyncio.sleep(0.1)
            agent.cancel()
            result = await task
            await agent.agent.wait_idle()
            return result

        result = asyncio.run(_scenario())
        assert result["final_response"] == ""
        assert result["completed"] is False
        assert result["error"] == "用户已手动停止"
        contents = [m.get("content") for m in result["messages"]]
        assert contents.count("streamed-partial") == 1
```

（`make_tool_call_response` 在用例内从 `tests.conftest` 导入，本文件顶部已导入 `MockLLMProvider`/`make_text_response`，需补 `from tests.conftest import make_tool_call_response`。）

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_agent_adapter.py::TestNewKernelPath::test_cancel_keeps_partial_streamed_content -v`

Expected: FAIL —— `assert contents.count("streamed-partial") == 1`（当前为 0，因为取消分支只拷贝 `state.messages`，流式片段不在其中）。

- [ ] **Step 3: 最小实现——三处小改动**

3a. `agent/core/agent_adapter.py` `__init__`（299 行 `self._final_response = ""` 之后）加：

```python
        self._partial_response = ""
```

3b. `run_conversation_async` 重置块（540 行 `self._final_response = ""` 之后）加：

```python
        self._partial_response = ""
```

3c. `_call_llm_hook` 的 `_stream_cb`（798-808 行）——`_parts.append(chunk)` 之后追加累积行：

```python
            def _stream_cb(
                chunk: str,
                _parts: list[str] = content_parts,
                _loop: asyncio.AbstractEventLoop = loop,
            ) -> None:
                _parts.append(chunk)
                self._partial_response = "".join(_parts)
                partial = {"role": "assistant", "content": "".join(_parts)}
                asyncio.run_coroutine_threadsafe(
                    _safe_emit(MessageUpdate(message=partial, delta=chunk, reasoning_delta=None)),
                    _loop,
                )
```

3d. `except asyncio.CancelledError:` 分支（645-648 行）——在 `self._result_messages = list(self._agent.state.messages)` 之后、`AgentEnd` emit 之前追加：

```python
            except asyncio.CancelledError:
                self._error = self._error or "用户已手动停止"
                self._result_messages = list(self._agent.state.messages)
                if self._partial_response:
                    self._result_messages.append({"role": "assistant", "content": self._partial_response})
                await self._agent._emit(AgentEnd(list(self._result_messages)))
```

**不改** `_final_response`（保持空）与 generic `except Exception` 分支（spec：只补取消路径）。

- [ ] **Step 4: 跑测试确认通过 + 存量回归**

Run: `.venv/bin/python -m pytest tests/test_agent_adapter.py -v`

Expected: PASS（TestNewKernelPath 全部用例，含新增 `test_cancel_keeps_partial_streamed_content`）。再跑相关存量：

Run: `.venv/bin/python -m pytest tests/test_chat.py tests/test_agent_loop.py -q`

Expected: PASS，`0 failed`。

- [ ] **Step 5: Commit**

```bash
git add agent/core/agent_adapter.py tests/test_agent_adapter.py
git commit -m "feat: persist partial streamed content on cancel"
```

---

## Task 3: M-3 —— 截断（桥）工具卡片 args 回退 + subtitle 增强

**Files:**
- Modify: `web/src/components/ToolStepCard.tsx:117-122`（`effectiveArgs` 回退链 + `extractSubtitle` 桥工具 case）

**Interfaces:**
- Consumes: `ToolStepCard` 现有 props（`call: ToolCall`、`result?: Message`）；`isPendingTool` 现有判定（104 行）；`extractSubtitle(toolName, args) -> string | null` 现有签名。
- Produces: `effectiveArgs` 语义变化——pending 卡 `result.content` 为空/空白时回退到 `call.function.arguments`；`extractSubtitle` 对 `tool_search`/`tool_describe`/`tool_call` 返回 `query`/`name` 摘要。无后端改动。

- [ ] **Step 1: 修改 `effectiveArgs` 为回退链**

`web/src/components/ToolStepCard.tsx:119` 替换为：

```ts
  const effectiveArgs = (() => {
    if (isPendingTool && result) {
      if (typeof result.content === "string" && result.content.trim()) return result.content;
      return call.function.arguments;   // 回退：pending 内容为空/空白时用 LLM 原始参数
    }
    return call.function.arguments;
  })();
```

- [ ] **Step 2: `extractSubtitle` 增加桥工具 case**

在 `extractSubtitle`（74-100 行）的 `if (toolName === "web_extract")` 块（90-92 行）之后、`query_`/`nc_` 前缀分支（93-98 行）之前插入：

```ts
  if (toolName === "tool_search") {
    return String(parsed.query || "").slice(0, 200) || null;
  }
  if (toolName === "tool_describe" || toolName === "tool_call") {
    return String(parsed.name || "").slice(0, 200) || null;
  }
```

- [ ] **Step 3: 前端门禁验证**

Run: `cd web && npm run build && npm run lint`

Expected: 无 TS 错误、`0 errors`。手工走查（spec §5.2）：对话中让 LLM 触发 `tool_search`/`tool_describe`/`tool_call`，确认卡片显示 query/name 摘要行（参数为空时不再整卡无信息）。

- [ ] **Step 4: Commit**

```bash
git add web/src/components/ToolStepCard.tsx
git commit -m "feat: fall back to raw args and subtitle for bridge tool cards"
```

---

## Task 4: M-5 —— 压缩 PhaseChange 补 session_id

**Files:**
- Modify: `agent/core/agent_adapter.py:879,903`（`_transform_context_hook` 两处 `_set_phase`）
- Test: `tests/test_agent_adapter.py`（新增用例）

**Interfaces:**
- Consumes: `PhaseChangeEvent`（`agent/events/types.py`，零改动）；`self._session_id`（`run_conversation_async` 入口已赋值）；`CompactionSettings`（`agent/context_compactor.py`）。
- Produces: 压缩路径两处 `PhaseChangeEvent` 的 `session_id` 非空（与同文件其他 `_set_phase` 调用点一致）。

- [ ] **Step 1: 写失败测试**

在 `tests/test_agent_adapter.py` 的 `TestNewKernelPath` 中（`test_new_compaction_hook_fires` 用例之后）新增：

```python
    def test_compaction_phase_change_has_session_id(self, monkeypatch):
        from agent.context_compactor import CompactionSettings
        from agent.events.bus import event_bus
        from agent.events.types import PhaseChangeEvent

        long_user = "长" * 400  # ~333 tokens under the character heuristic

        class _SpyProvider:
            def __init__(self, inner):
                self.inner = inner
                self.messages: list[list[dict]] = []

            def chat(self, **kwargs):
                self.messages.append(list(kwargs.get("messages", [])))
                return self.inner.chat(**kwargs)

        inner = MockLLMProvider(responses=[make_text_response("summary"), make_text_response("ok")])
        spy = _SpyProvider(inner)
        agent = AIAgent(
            api_key="sk-fake",
            base_url="x",
            model="gpt-4o",
            max_iterations=3,
            compaction_settings=CompactionSettings(
                enabled=True, max_context_tokens=120, reserve_tokens=20, keep_recent_tokens=50
            ),
        )
        agent._llm = LLMClient(api_key="sk-fake", base_url="x", provider=spy)

        captured: list[PhaseChangeEvent] = []
        event_bus.subscribe(lambda e: captured.append(e) if isinstance(e, PhaseChangeEvent) else None)

        result = agent.run_conversation(long_user, session_id="chat-42")
        assert result["completed"] is True
        phases = [p.to_phase for p in captured]
        assert "compaction" in phases
        for p in captured:
            assert p.session_id == "chat-42"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_agent_adapter.py::TestNewKernelPath::test_compaction_phase_change_has_session_id -v`

Expected: FAIL —— `assert p.session_id == "chat-42"`（压缩两处 `_set_phase` 未传 `session_id`，为 `""`）。

- [ ] **Step 3: 最小实现——两处补 `session_id`**

`agent/core/agent_adapter.py` 879 行：

```python
self._set_phase(
    AgentPhase.COMPACTION, f"context over threshold ({total_est} > {threshold})", session_id=self._session_id
)
```

903 行：

```python
        self._set_phase(AgentPhase.IDLE, f"compaction saved {saved} tokens", session_id=self._session_id)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_agent_adapter.py -v`

Expected: PASS（含新增用例；既有 `test_new_session_id_propagates_to_events` 仍绿）。

- [ ] **Step 5: Commit**

```bash
git add agent/core/agent_adapter.py tests/test_agent_adapter.py
git commit -m "fix: propagate session_id on compaction phase changes"
```

---

## Task 5: M-6 —— Agent 复用防御（token 采纳 + subscribe 幂等）

**Files:**
- Modify: `agent/core/agent.py:114-115`（`run_async` token 采纳）、`agent/core/agent.py:60-68`（`subscribe`）
- Test: `tests/test_agent.py`（新增 3 用例 + 修改 `test_cancel_and_wait_idle`）

**Interfaces:**
- Consumes: `CancelToken`（`agent/core/kernel_types.py`）；`Agent.run_async(prompts, config, token=None) -> list[dict]` 现有签名；`Agent.subscribe(listener) -> Callable[[], None]` 现有签名（返回 unsubscribe）。
- Produces: `run_async` 语义变化——显式传入的 `token` **始终采纳**（即使内部 token 已取消）；未传 token 且内部 token 已取消时自动重置为新 `CancelToken()`，使实例可复用。`subscribe` 幂等——同一 listener 重复订阅为 no-op。

- [ ] **Step 1: 修改 `test_cancel_and_wait_idle`（旧行为即 M-6 footgun）**

`tests/test_agent.py:106-121` 的 `test_cancel_and_wait_idle` 整体替换为"运行中取消"的确定性版本（保留测试意图：取消的 run 抛 `asyncio.CancelledError` 且 `wait_idle` 可等待）：

```python
def test_cancel_and_wait_idle():
    import time

    def sleepy(args: dict) -> str:
        time.sleep(0.5)
        return tool_result()

    registry.register(name="sleeper", toolset="test", schema={"type": "object"}, handler=sleepy)
    agent = Agent()
    token = CancelToken()

    async def _scenario():
        task = asyncio.create_task(
            agent.run_async(
                [{"role": "user", "content": "go"}],
                _cfg(LLMResponse(content="", tool_calls=[ToolCallPayload(id="c1", name="sleeper", arguments="{}")])),
                token,
            )
        )
        await asyncio.sleep(0.05)
        token.cancel()  # cancel the in-use token mid-run
        await agent.wait_idle()
        assert agent.state.running is False
        with pytest.raises(asyncio.CancelledError):
            await task

    _run(_scenario())
```

- [ ] **Step 2: 写失败测试（新增 3 用例，追加在文件末尾）**

```python
def test_run_async_accepts_fresh_token_after_cancel():
    """M-6: an explicitly passed fresh token must be adopted even when the
    instance's internal token was previously cancelled."""
    agent = Agent()
    agent.cancel()  # internal token now cancelled
    fresh = CancelToken()
    result = _run(agent.run_async([{"role": "user", "content": "hi"}], _cfg(LLMResponse(content="ok")), fresh))
    assert [m["content"] for m in result] == ["hi", "ok"]


def test_run_async_resets_token_after_cancel_without_argument():
    """M-6: with no token passed, a previously cancelled internal token is
    replaced so the Agent instance can be reused."""
    agent = Agent()
    agent.cancel()
    result = _run(agent.run_async([{"role": "user", "content": "hi"}], _cfg(LLMResponse(content="ok"))))
    assert [m["content"] for m in result] == ["hi", "ok"]


def test_subscribe_is_idempotent():
    """M-6: subscribing the same listener twice must register it once."""
    agent = Agent()
    seen: list[str] = []
    listener = lambda e: seen.append(e.type)  # noqa: E731
    unsub = agent.subscribe(listener)
    agent.subscribe(listener)  # duplicate — must be a no-op
    assert agent._listeners.count(listener) == 1
    _run(agent._emit(MessageStart(message={"role": "user"})))
    assert seen == ["message_start"]
    unsub()
    _run(agent._emit(MessageEnd(message={"role": "user"})))
    assert seen == ["message_start"]
```

- [ ] **Step 3: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_agent.py -v`

Expected: FAIL —— `test_run_async_accepts_fresh_token_after_cancel` 与 `test_run_async_resets_token_after_cancel` 超时/断言失败（run 被旧取消态中断抛 `CancelledError`），`test_subscribe_is_idempotent` 断言 `== 1` 失败（实际 2）。

- [ ] **Step 4: 最小实现**

`agent/core/agent.py:114-115` 替换为：

```python
        if token is not None:
            self._token = token
        elif self._token.cancelled:
            self._token = CancelToken()
```

`agent/core/agent.py:60-68` 的 `subscribe` 替换为：

```python
    def subscribe(self, listener: Callable[[AgentEvent], None]) -> Callable[[], None]:
        """Register a sync listener.  Idempotent: re-subscribing the same
        listener is a no-op.  Returns an unsubscribe callable."""
        if listener not in self._listeners:
            self._listeners.append(listener)

        def _unsub() -> None:
            if listener in self._listeners:
                self._listeners.remove(listener)

        return _unsub
```

- [ ] **Step 5: 跑测试确认通过 + 回归**

Run: `.venv/bin/python -m pytest tests/test_agent.py -v`

Expected: PASS（9 passed，含修改后的 `test_cancel_and_wait_idle`）。

Run: `.venv/bin/python -m pytest tests/test_agent_adapter.py tests/test_agent_loop.py -q`

Expected: PASS，`0 failed`（适配层取消/steering 用例不受影响）。

- [ ] **Step 6: Commit**

```bash
git add agent/core/agent.py tests/test_agent.py
git commit -m "fix: adopt new token on run and make subscribe idempotent"
```

---

## Task 6: M-7 —— 删除同步 `dispatch_tool` 死代码

**Files:**
- Modify: `agent/core/tool_dispatcher.py:1-12`（docstring）、`:62-80`（`dispatch_tool` 函数）、`:338`（`__all__`）
- Modify: `agent/core/__init__.py:14,22`（re-export 与 `__all__`）

**Interfaces:**
- Consumes: 无（grep 全文已确认除定义本身与 `__init__.py` re-export 外无任何引用；`_truncate` 仍被 batch 路径使用，保留）。
- Produces: `agent.core` 不再导出 `dispatch_tool`；`dispatch_tool_batch` 导出不变。

- [ ] **Step 1: 删除 `dispatch_tool` 与 re-export**

1a. `agent/core/tool_dispatcher.py` docstring（1-12 行）替换为（去掉 sync path / `dispatch_tool` 表述）：

```python
"""Tool dispatch — async batch execution.

Pi-style kernel redesign (spec 2026-08-06-pi-style-kernel-design).
:func:`dispatch_tool_batch` runs every tool through ``registry.dispatch``
in a worker thread so the security before/after hook chain and the
``__block__`` protocol stay intact (Layer 2), then results are truncated
per entry and returned in original order.  Batches run in parallel,
degrading to strict sequential execution when a batch contains an
``execution_mode == "sequential"`` tool.
"""
```

1b. 删除整个 `dispatch_tool` 函数（62-80 行，含函数前的空行；保留 82 行 `async def _await_maybe` 之前的空行结构）。

1c. 末尾 `__all__`（338 行）改为：

```python
__all__ = ["dispatch_tool_batch"]
```

1d. `agent/core/__init__.py`：删除 14 行 `from agent.core.tool_dispatcher import dispatch_tool`；`__all__` 删除 22 行 `    "dispatch_tool",`。

- [ ] **Step 2: 验证 import 干净 + 全量回归**

Run: `.venv/bin/python -c "import agent.core; print('agent.core import ok')"`

Expected: `agent.core import ok`（无 ImportError）。

Run: `.venv/bin/python -m pytest tests/ -q && ruff check .`

Expected: `>= 501 passed`，`0 failed`；`ruff check .` 无输出。

- [ ] **Step 3: Commit**

```bash
git add agent/core/tool_dispatcher.py agent/core/__init__.py
git commit -m "refactor: remove dead sync dispatch_tool"
```

---

# B 组 —— TAVILY_API_KEY 运行时 .env（不打包）

## Task 7: B —— `~/.zlink-agent/.env` 写入 + `backend/config.py` 候选加载

**Files:**
- Modify: `backend/config.py`（全文，13 行）
- Create: `tests/test_backend_config_env.py`
- 外部动作：向 `~/.zlink-agent/.env` 追加用户提供的真实 key（spec 不写真实值）

**Interfaces:**
- Consumes: `DATA_DIR`（`agent/utils.py`，模块级已解析，`~/.zlink-agent/data`，仅标准库依赖无循环导入）；`load_dotenv`（`python-dotenv`，已在依赖中）。
- Produces: `backend.config._load_env_files() -> None`——依次加载 `DATA_DIR.parent / ".env"`（用户级/打包版）与仓库根 `.env`（dev），`override=False`（环境变量 > 先加载文件 > 后加载文件）。`HOST`/`PORT`/`CORS_ORIGINS` 读取行为不变。`agent/tools/web_tools.py` 的 `os.getenv("TAVILY_API_KEY")` 零改动。

- [ ] **Step 1: 写失败测试**

Create `tests/test_backend_config_env.py`:

```python
"""Tests for backend/config.py — _load_env_files candidate loading (B)."""

from __future__ import annotations

import os


def test_loads_user_level_env_before_root(monkeypatch, tmp_path):
    import backend.config as cfg

    env_dir = tmp_path / "data"
    env_dir.mkdir()
    (env_dir.parent / ".env").write_text("TAVILY_TEST_KEY=from-user-file\n", encoding="utf-8")
    monkeypatch.setattr(cfg, "DATA_DIR", env_dir)
    monkeypatch.delenv("TAVILY_TEST_KEY", raising=False)

    cfg._load_env_files()

    assert os.environ.get("TAVILY_TEST_KEY") == "from-user-file"


def test_existing_environment_variable_wins(monkeypatch, tmp_path):
    import backend.config as cfg

    env_dir = tmp_path / "data"
    env_dir.mkdir()
    (env_dir.parent / ".env").write_text("TAVILY_TEST_KEY=from-file\n", encoding="utf-8")
    monkeypatch.setattr(cfg, "DATA_DIR", env_dir)
    monkeypatch.setenv("TAVILY_TEST_KEY", "from-env")

    cfg._load_env_files()

    assert os.environ.get("TAVILY_TEST_KEY") == "from-env"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_backend_config_env.py -v`

Expected: FAIL —— `AttributeError: module 'backend.config' has no attribute '_load_env_files'`。

- [ ] **Step 3: 最小实现——重写 `backend/config.py`**

整文件替换为：

```python
"""Server-level configuration."""

import os
from pathlib import Path

from dotenv import load_dotenv

from agent.utils import DATA_DIR


def _load_env_files() -> None:
    """Load .env candidates in order (override=False — env wins).

    User-level ``~/.zlink-agent/.env`` (``DATA_DIR.parent``) is the
    packaged/production source for keys like TAVILY_API_KEY (never bundled
    into the PyInstaller build); the repo-root ``.env`` remains the dev
    fallback.
    """
    candidates = [
        DATA_DIR.parent / ".env",  # ~/.zlink-agent/.env
        Path(__file__).resolve().parent.parent / ".env",  # repo root (dev)
    ]
    for p in candidates:
        if p.exists():
            load_dotenv(p, override=False)


_load_env_files()

HOST = os.environ.get("ZLINK_AGENT_HOST", "0.0.0.0")
PORT = int(os.environ.get("ZLINK_AGENT_PORT", "8089"))
CORS_ORIGINS = os.environ.get("ZLINK_AGENT_CORS", "http://localhost:8088").split(",")
```

- [ ] **Step 4: 跑测试确认通过 + 回归**

Run: `.venv/bin/python -m pytest tests/test_backend_config_env.py -v`

Expected: PASS（2 passed）。

Run: `.venv/bin/python -m pytest tests/ -q`

Expected: `>= 501 passed`，`0 failed`（`test_web_tools.py` 的 `test_uses_duckduckgo_by_default` 已 `monkeypatch.delenv("TAVILY_API_KEY")` 隔离环境变量，不受影响）。

- [ ] **Step 5: 写入用户级 `.env`（用户提供真实 key）**

在 shell 中执行（将 `<USER_PROVIDED_KEY>` 替换为用户提供的真实 Tavily key，格式 `tvly-...`；spec 明确不写真实值，此为用户输入点而非计划占位符）：

```bash
mkdir -p ~/.zlink-agent
grep -q '^TAVILY_API_KEY=' ~/.zlink-agent/.env 2>/dev/null || printf 'TAVILY_API_KEY=tvly-%s\n' '<USER_PROVIDED_KEY>' >> ~/.zlink-agent/.env
```

验证 key 可被后端加载（无需重启；子进程内验证）：

Run: `.venv/bin/python -c "import backend.config; import os; print('key loaded:', bool(os.getenv('TAVILY_API_KEY')))"`

Expected: `key loaded: True`

- [ ] **Step 6: 打包冒烟 + 打包版人工验证（B 组验收）**

Run: `bash scripts/build-pyinstaller.sh`

Expected: 脚本自带冒烟通过——`✅ 冒烟测试通过`（后端启动 + `/api/system/version` 响应正常 + 工具注册数量 ≥ 50 + NC 工具已注册）。

人工验证：启动打包版（Electron 或 `dist/` 内二进制），对话触发 `web_search`，确认命中 Tavily 档（返回结果而非"搜索暂不可用"）。注意：`~/.zlink-agent/.env` 在用户数据目录、天然不进 git、不进包；dmg/exe 中无明文密钥。

- [ ] **Step 7: Commit**

```bash
git add backend/config.py tests/test_backend_config_env.py
git commit -m "feat: load env from user data dir .env at startup (packaged builds)"
```

---

# C 组 —— 前端优化（每批 npm run build + npm run lint 门禁）

## Task 8: C-1 —— 宽屏布局（page-container 1280px + card-grid + form-grid-2）

**Files:**
- Modify: `web/src/styles/global.css:1167-1170`（`.page-container`）、（新增 `.card-grid`/`.form-grid-2` 等规则）
- Modify: `web/src/pages/HistoryPage.tsx:62-63,87-88`（会话卡包 `card-grid`）
- Modify: `web/src/pages/MemoryPage.tsx:71`（`style` → `card-grid`，顺带消除 1 处 inline）
- Modify: `web/src/pages/ToolsPage.tsx:94`（同上）
- Modify: `web/src/pages/SettingsLLMPage.tsx:84,106`（两个 `.card` 加 `form-grid-2`）
- Modify: `web/src/pages/SettingsAgentPage.tsx:50,91`（同上）
- Modify: `web/src/pages/SettingsERPPage.tsx:320`（ErpTabPanel 的 `.card` 加 `form-grid-2`）

**Interfaces:**
- Consumes: 现有 `.card`（`margin-bottom: 12px`，global.css:1212）、`.form-group`（`margin-bottom: 16px`，global.css:1396）、`.skill-grid`（已 `repeat(auto-fill, minmax(340px, 1fr))`）、`.toolset-group`。
- Produces: CSS 类 `.card-grid`、`.form-grid-2`（及网格内间距/跨列规则）；`@tabler/icons-react` 零新增。

**先后关系（避免与 C-2 冲突）：**
- MemoryPage:71 与 ToolsPage:94 的 inline style 在本任务被 `card-grid` 替换——Task 10/11 的 C-2 映射表**不包含**这两行。
- SkillManagerPage 已用 `.skill-grid`（340px auto-fill 多列网格），CronJobPage 已核实为 `<table className="data-table">`（数据表，非卡片列表）——**两页均不套 `card-grid`**（表格套网格会破坏布局，违背 spec R4"行为不变"）。SettingsExtensionsPage 已是 `skill-grid` 卡片网格，仅受益于更宽容器。
- `.form-grid-2` 作用于 `.card` 元素本身；`.card-actions`/`.form-actions` 通过 `grid-column: 1 / -1` 跨列；`.test-result-ok/error` 的跨列规则在 Task 12 (C-3) 添加（规则引用尚未定义的类无害）。

- [ ] **Step 1: global.css 布局类**

1a. `.page-container`（1167-1170 行）`max-width: 1040px` → `1280px`：

```css
.page-container {
  max-width: 1280px;
  margin: 0 auto;
}
```

1b. 在 `.card-actions` 规则（1246-1250 行）之后插入：

```css
/* ── Responsive layouts (C-1) ─────────────────────────── */

.card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  gap: 12px;
}
.card-grid > * { margin-bottom: 0; }  /* 卡片间距由 gap 承担，抵消 .card 的 margin-bottom */

.form-grid-2 {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
  gap: 16px 24px;
}
.form-grid-2 > .form-group { margin-bottom: 0; }  /* 行距由 gap 承担 */
.form-grid-2 > .card-header,
.form-grid-2 > .card-actions,
.form-grid-2 > .form-actions {
  grid-column: 1 / -1;
}
```

- [ ] **Step 2: HistoryPage 会话卡包 `card-grid`**

`web/src/pages/HistoryPage.tsx:62-63`：

```tsx
      ) : (
        <div className="card-grid">
          {sessions.map((s) => {
```

`web/src/pages/HistoryPage.tsx:87-88`：

```tsx
          })}
        </div>
      )}
```

（中间 map 体缩进保持原样——eslint 配置不含 JSX 缩进规则，`npm run lint` 门禁验证；其余 3 处 inline style 由 Task 11 C-2 批次 3 与 Task 13 C-4 处理。）

- [ ] **Step 3: MemoryPage / ToolsPage 卡片容器**

`web/src/pages/MemoryPage.tsx:71`：

```tsx
      <div className="card-grid">
```

（原 `<div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>` 整段替换为 `<div className="card-grid">`。）

`web/src/pages/ToolsPage.tsx:94`：

```tsx
        <div className="card-grid">
```

（同样替换。）

- [ ] **Step 4: 三个配置页 `.card` 加 `form-grid-2`**

- `SettingsLLMPage.tsx:84`：`<div className="card">` → `<div className="card form-grid-2">`（只读卡）
- `SettingsLLMPage.tsx:106`：`<div className="card">` → `<div className="card form-grid-2">`（编辑卡）
- `SettingsAgentPage.tsx:50`：`<div className="card">` → `<div className="card form-grid-2">`（只读卡）
- `SettingsAgentPage.tsx:91`：`<div className="card">` → `<div className="card form-grid-2">`（编辑卡）
- `SettingsERPPage.tsx:320`：`<div className="card">` → `<div className="card form-grid-2">`（ErpTabPanel）

（每处 `className="card"` 在文件中出现多次，按上述行号定位替换；替换前可用 `sed -n '<行号>p' <文件>` 确认目标行内容。）

- [ ] **Step 5: 前端门禁验证**

Run: `cd web && npm run build && npm run lint`

Expected: 无 TS 错误、`0 errors`。手工走查（spec §5.2）：宽屏（≥1280px）下列表页多列/配置页两栏，窄屏回落单列；CronJobPage 表格、McpPage 结构无变化。

- [ ] **Step 6: Commit**

```bash
git add web/src/styles/global.css web/src/pages/HistoryPage.tsx web/src/pages/MemoryPage.tsx web/src/pages/ToolsPage.tsx web/src/pages/SettingsLLMPage.tsx web/src/pages/SettingsAgentPage.tsx web/src/pages/SettingsERPPage.tsx
git commit -m "feat: widen page container and add responsive card/form grids"
```

---

## Task 9: C-2 批次 1 —— McpPage.tsx 内联样式收敛（51 处）

**Files:**
- Modify: `web/src/styles/global.css`（新增 C-2 工具类，全批次共享）
- Modify: `web/src/pages/McpPage.tsx`（51 处 `style={{}}` → CSS 类）

**Interfaces:**
- Consumes: `global.css` 既有 `--` 变量体系（`:root` 已定义 `--primary`/`--success`/`--warning`/`--danger`/`--bg-*`/`--text-*`/`--border`/`--radius*`/`--font-*`）。
- Produces: C-2 工具类全集（Task 10/11 复用，签名冻结——本任务 Step 1 的 CSS 块是全批次的唯一事实源）。

**迁移规则（spec §4 C-2）：**
1. 每个类体 = 被替换 inline style 的**逐字复制**（含 `var(--bg-2)` 等既有未定义引用，保持当前计算行为，零视觉变化）。
2. 动态样式（依赖 state/运行时值）保持 inline 并在代码中留 `// dynamic:` 注释。
3. 一次性静态样式（如 `display: "none"` 的 file input、skeleton 尺寸）保持 inline——无复用价值，不建一次性类。

- [ ] **Step 1: 新增 C-2 工具类到 global.css**

在 `global.css` 末尾（`.data-table` 区块之后、文件结尾）追加（本块是全批次共享的唯一事实源，Task 10/11 直接复用，不再重复贴）：

```css
/* ── Utility classes (C-2: inline-style consolidation) ── */

/* layout containers */
.toolbar { display: flex; align-items: center; gap: 12px; margin-bottom: 20px; flex-wrap: wrap; }
.toolbar-lg { display: flex; align-items: center; gap: 16px; margin-bottom: 20px; flex-wrap: wrap; }
.toolbar-md { display: flex; align-items: center; gap: 12px; margin-bottom: 16px; flex-wrap: wrap; }
.flex-col-gap { display: flex; flex-direction: column; gap: 12px; }
.flex-col-gap-sm { display: flex; flex-direction: column; gap: 8px; }
.flex-row-gap { display: flex; gap: 12px; }
.flex-row-gap-sm { display: flex; gap: 8px; }
.flex-row-gap-6 { display: flex; align-items: center; gap: 6px; }
.flex-between { display: flex; align-items: center; justify-content: space-between; margin-bottom: 12px; }
.radio-label { display: flex; align-items: center; gap: 6px; cursor: pointer; font-size: 14px; }
.row-actions { margin-top: 12px; display: flex; gap: 8px; }
.grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
.grid-2-sm { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }

/* text */
.text-meta { font-size: 13px; color: var(--text-3); }
.text-hint { font-size: 12px; color: var(--text-3); }
.text-hint-mt { font-size: 12px; color: var(--text-3); margin-top: 4px; }
.text-tiny { font-size: 11px; color: var(--text-4); }
.text-tiny-mt { font-size: 11px; color: var(--text-4); margin-top: 4px; }
.text-name { font-size: 14px; font-weight: 600; }
.text-muted { color: var(--text-3); }
.text-muted-mt { font-size: 13px; color: var(--text-2); margin-top: 2px; }
.text-sep { color: var(--border); }
.text-success { color: var(--success); font-weight: 600; }

/* spacing */
.mt-sm { margin-top: 4px; }
.mb-sm { margin-bottom: 8px; }
.mb-md { margin-bottom: 12px; }
.mb-lg { margin-bottom: 16px; }
.mr-sm { margin-right: 8px; }
.ml-sm { margin-left: 8px; }
.p-md { padding: 16px; }
.p-lg { padding: 24px; }

/* controls */
.icon-btn-sm { height: 28px; width: 28px; padding: 0; justify-content: center; }
.icon-btn-xs { height: 26px; padding: 0 8px; font-size: 11px; }
.seg-btn { height: 28px; padding: 0 10px; font-size: 12px; }
.seg-group { display: flex; gap: 4px; background: var(--bg-2); border-radius: 6px; padding: 2px; }
.seg-group-page { display: flex; gap: 4px; background: var(--bg-page); border-radius: 6px; padding: 2px; }
.btn-xs { font-size: 12px; padding: 4px 12px; }
.badge-xs { font-size: 11px; }
.toast-close { margin-left: auto; background: none; border: none; cursor: pointer; color: inherit; }

/* misc */
.mono-input { font-family: var(--mono-font, monospace); font-size: 12px; resize: vertical; min-height: 260px; }
.editor-textarea { width: 100%; min-height: 300px; font-family: var(--mono-font, monospace); font-size: 12px; padding: 8px; border-radius: 6px; border: 1px solid var(--border); background: var(--bg); color: var(--text); resize: vertical; }
.chip { padding: 8px 12px; background: var(--bg-hover); border-radius: var(--radius-sm); font-size: 13px; color: var(--text-2); }
.section-pad { padding: 8px 16px 12px; }
.list-row { padding: 6px 0; border-bottom: 1px solid var(--border-light); }
.card-title-sm { margin: 0; font-size: 15px; font-weight: 600; }
.card-title-xs { margin: 0; font-size: 14px; font-weight: 600; }
.label-sm { font-size: 12px; color: var(--text-3); font-weight: 500; }
.chip-row { display: flex; gap: 8px; margin-top: 8px; flex-wrap: wrap; }
.tag-row { margin-top: 4px; display: flex; flex-wrap: wrap; gap: 4px; }
.empty-state-icon-dim { opacity: 0.3; }
.test-panel { margin-top: 8px; padding: 8px 12px; background: var(--bg-2); border-radius: 6px; font-size: 13px; }
.panel-pad { margin-top: 12px; padding: 12px; background: var(--bg-2); border-radius: 6px; }
.dot-success { width: 8px; height: 8px; border-radius: 50%; background: var(--success); flex-shrink: 0; }
.iter-value { font-size: 14px; font-weight: 600; color: var(--text-1); min-width: 24px; }
.value-label { font-size: 13px; font-weight: 600; margin-left: 8px; }
.skill-card-desc-pad { padding: 0 14px 14px; font-size: 12px; color: var(--text-3); line-height: 1.6; }
.footnote { margin-top: 24px; font-size: 12px; color: var(--text-3); line-height: 1.6; }
.footnote-list { margin-top: 6px; padding-left: 20px; }
.page-intro { margin: 0 0 20px; color: var(--text-2); line-height: 1.6; font-size: 14px; }
.erp-tabs { display: inline-flex; background: var(--bg-card); border: 1px solid var(--border); border-radius: var(--radius); padding: 4px; margin-bottom: 20px; gap: 4px; }
.card-subtitle-sm { margin: 4px 0 0; font-size: 12px; color: var(--text-3); }
.field-value { padding: 8px 0; font-size: 14px; }
.error-inline { color: var(--danger); margin-bottom: 12px; }
.job-edit-card { margin: 8px; padding: 16px; }
.link-btn { margin-left: 6px; font-size: 12px; color: var(--primary); background: none; border: none; cursor: pointer; text-decoration: underline; padding: 0; }
```

- [ ] **Step 2: McpPage 逐行迁移（51 处）**

对 `web/src/pages/McpPage.tsx` 按下列映射逐行操作（`style={{...}}` 替换为对应 `className`；`<button className="btn btn-ghost" ...>` 等已有 className 的追加类名；无 className 的元素新增）：

| 行号 | 当前 `style={{}}` | 替换为 |
|------|-------------------|--------|
| 224 | `{ display:"flex", alignItems:"center", gap:"12px", marginBottom:"16px", flexWrap:"wrap" }` | `className="toolbar-md"` |
| 229 | `{ animation: reloading ? "spin 1s linear infinite" : undefined }` | 保持 inline（`// dynamic: reloading`） |
| 232 | `{ fontSize:"13px", color:"var(--text-3)" }` | `className="text-meta"` |
| 236 | `{ marginBottom:"12px" }` | 追加 `mb-md`（`className="toast toast-error mb-md"`） |
| 238 | `{ marginLeft:"auto", background:"none", border:"none", cursor:"pointer", color:"inherit" }` | `className="toast-close"` |
| 243 | `{ marginBottom:"16px", padding:"16px" }` | 追加 `mb-lg p-md` |
| 244 | `{ display:"flex", alignItems:"center", justifyContent:"space-between", marginBottom:"12px" }` | `className="flex-between"` |
| 245 | `{ margin:0, fontSize:"15px", fontWeight:600 }` | `className="card-title-sm"` |
| 246 | `{ display:"flex", gap:"4px", background:"var(--bg-2)", borderRadius:"6px", padding:"2px" }` | `className="seg-group"` |
| 249 | `{ height:"28px", padding:"0 10px", fontSize:"12px" }` | 追加 `seg-btn` |
| 256 | 同上 | 追加 `seg-btn` |
| 264 | `{ marginBottom:"16px" }` | `className="mb-lg"` |
| 265 | `{ fontSize:"12px", color:"var(--text-3)", fontWeight:500 }` | `className="label-sm"` |
| 266 | `{ display:"flex", gap:"8px", marginTop:"8px", flexWrap:"wrap" }` | `className="chip-row"` |
| 267 | `{ fontSize:"12px", padding:"4px 12px" }` | 追加 `btn-xs` |
| 274 | 同上 | 追加 `btn-xs` |
| 281 | 同上 | 追加 `btn-xs` |
| 292 | `{ display:"grid", gridTemplateColumns:"1fr 1fr", gap:"16px" }` | `className="grid-2"` |
| 332 | `{ fontFamily:"var(--mono-font, monospace)", fontSize:"12px", resize:"vertical", minHeight:"260px" }` | `className="mono-input"` |
| 344 | `{ fontSize:"11px", color:"var(--text-4)", marginTop:"4px" }` | `className="text-tiny-mt"` |
| 350 | `{ marginTop:"12px", display:"flex", gap:"8px" }` | `className="row-actions"` |
| 362 | `{ width:"60%", marginBottom:24 }` | 保持 inline（skeleton 一次性尺寸） |
| 368 | `{ opacity:0.3 }` | 追加 `empty-state-icon-dim` |
| 372 | `{ display:"flex", flexDirection:"column", gap:"12px" }` | `className="flex-col-gap"` |
| 377-383 | 状态点（宽高 8 + **动态颜色**） | 保持 inline（`// dynamic: statusColor`） |
| 384 | `{ fontSize:"14px", fontWeight:600 }` | `className="text-name"` |
| 386 | `{ fontSize:11 }` | 追加 `badge-xs` |
| 388 | `{ fontSize:"11px", color:"var(--text-4)" }` | `className="text-tiny"` |
| 389 | `{ fontSize:"12px", color: statusColor(s.status) }` | 保持 inline（`// dynamic: statusColor`） |
| 391 | `{ fontSize:"12px", color:"var(--text-3)" }` | `className="text-hint"` |
| 397 | `{ height:"28px", width:"28px", padding:"0", justifyContent:"center" }` | 追加 `icon-btn-sm` |
| 406 | 同上 | 追加 `icon-btn-sm` |
| 415 | 同上 | 追加 `icon-btn-sm` |
| 424 | 同上 | 追加 `icon-btn-sm` |
| 433 | 同上 | 追加 `icon-btn-sm` |
| 444 | `{ color:"var(--error)" }` | 保持 inline（`// --error 未定义，当前为继承父级颜色，保持现状`） |
| 448 | `{ marginTop:"8px", padding:"8px 12px", background:"var(--bg-2)", borderRadius:"6px", fontSize:"13px" }` | `className="test-panel"` |
| 451 | `{ color:"var(--success)", fontWeight:600 }` | `className="text-success"` |
| 452 | `{ color:"var(--text-3)", marginLeft:"8px" }` | `className="text-muted ml-sm"` |
| 457 | `{ marginLeft:"8px" }` | `className="ml-sm"` |
| 464 | `{ marginTop:"4px", display:"flex", flexWrap:"wrap", gap:"4px" }` | `className="tag-row"` |
| 472 | `{ color:"var(--error)" }` | 保持 inline（同上注释） |
| 478 | `{ marginTop:"12px", padding:"12px", background:"var(--bg-2)", borderRadius:"6px" }` | `className="panel-pad"` |
| 479 | `{ display:"flex", alignItems:"center", justifyContent:"space-between", marginBottom:"12px" }` | `className="flex-between"` |
| 480 | `{ margin:0, fontSize:"14px", fontWeight:600 }` | `className="card-title-xs"` |
| 481 | `{ display:"flex", gap:"4px", background:"var(--bg-page)", borderRadius:"6px", padding:"2px" }` | `className="seg-group-page"` |
| 484 | `{ height:"26px", padding:"0 8px", fontSize:"11px" }` | 追加 `icon-btn-xs` |
| 491 | 同上 | 追加 `icon-btn-xs` |
| 504 | 同 332 | `className="mono-input"` |
| 510 | `{ display:"grid", gridTemplateColumns:"1fr 1fr", gap:"12px" }` | `className="grid-2-sm"` |
| 545 | 同 350 | `className="row-actions"` |

- [ ] **Step 3: 前端门禁验证**

Run: `cd web && npm run build && npm run lint`

Expected: 无 TS 错误、`0 errors`。手工走查：McpPage 各区块（添加表单、服务器列表、测试面板、编辑配置）视觉与迁移前一致。

- [ ] **Step 4: Commit**

```bash
git add web/src/styles/global.css web/src/pages/McpPage.tsx
git commit -m "refactor: consolidate McpPage inline styles into CSS classes"
```

---

## Task 10: C-2 批次 2 —— SkillManagerPage / MemoryPage / SettingsAgentPage（各 19 处）

**Files:**
- Modify: `web/src/pages/SkillManagerPage.tsx`、`web/src/pages/MemoryPage.tsx`、`web/src/pages/SettingsAgentPage.tsx`

**Interfaces:**
- Consumes: Task 9 Step 1 定义的工具类全集（`global.css`，本任务直接复用，不再新增类）。
- Produces: 无新 CSS。注意 MemoryPage:71 已被 Task 8 (C-1) 替换为 `card-grid`，本任务映射表不含该行。

- [ ] **Step 1: SkillManagerPage 逐行迁移（19 处）**

| 行号 | 当前 `style={{}}` | 替换为 |
|------|-------------------|--------|
| 110 | `{ display:"flex", alignItems:"center", gap:"16px", marginBottom:"20px", flexWrap:"wrap" }` | `className="toolbar-lg"` |
| 111 | `{ padding:"0 16px", cursor:"pointer" }` | 保持 inline（一次性按钮） |
| 114 | `{ display:"none" }` | 保持 inline（file input 隐藏惯例） |
| 116 | `{ flex:"1", minWidth:"200px" }` | 保持 inline（一次性布局） |
| 125 | `{ fontSize:"13px", color:"var(--text-3)" }` | `className="text-meta"` |
| 133 | `{ height:140, borderRadius:"var(--radius-lg)" }` | 保持 inline（skeleton 一次性尺寸） |
| 138 | `{ opacity:0.3 }` | 追加 `empty-state-icon-dim` |
| 147-153 | 状态点（宽高 8 + **动态颜色** `s.active ? ... : ...`） | 保持 inline（`// dynamic: s.active`） |
| 154 | `{ fontSize:"14px", fontWeight:600 }` | `className="text-name"` |
| 156 | `{ fontSize:"11px", color:"var(--text-4)" }` | `className="text-tiny"` |
| 159 | `{ fontSize:11 }` | 追加 `badge-xs` |
| 166 | `{ height:"28px", width:"28px", padding:"0", justifyContent:"center" }` | 追加 `icon-btn-sm` |
| 175 | 同上 | 追加 `icon-btn-sm` |
| 181-186 | `{ transition:"transform 0.2s", transform: ..., color: ... }` | 保持 inline（`// dynamic: expanded`） |
| 206 | `{ display:"flex", flexDirection:"column", gap:"8px" }` | `className="flex-col-gap-sm"` |
| 208-216 | textarea 编辑器样式 | `className="editor-textarea"` |
| 220 | `{ display:"flex", gap:"8px" }` | `className="flex-row-gap-sm"` |
| 231 | `{ display:"flex", gap:"4px", marginBottom:"8px" }` | 保持 inline（一次性按钮行） |
| 242 | `{ fontSize:"12px", color:"var(--text-4)" }` | `className="text-tiny"` |

- [ ] **Step 2: MemoryPage 逐行迁移（19 处）**

| 行号 | 当前 `style={{}}` | 替换为 |
|------|-------------------|--------|
| 56 | `{ height:60 }` | 保持 inline（skeleton 一次性尺寸） |
| 57 | `{ height:60 }` | 保持 inline |
| 58 | `{ height:200 }` | 保持 inline |
| 62 | `{ marginBottom:"16px", display:"flex", alignItems:"center", gap:"12px" }` | `className="toolbar-md"` |
| 63 | `{ fontSize:"13px", color:"var(--text-3)" }` | `className="text-meta"` |
| 67 | `{ color:"var(--border)" }` | `className="text-sep"` |
| 71 | （已由 Task 8 替换为 `card-grid`） | 不处理 |
| 82 | `{ padding:"8px 16px 12px" }` | `className="section-pad"` |
| 84 | `{ color:"var(--text-3)", fontSize:"13px" }` | `className="text-meta"` |
| 86 | `{ display:"flex", flexDirection:"column", gap:"8px" }` | `className="flex-col-gap-sm"` |
| 88 | `{ padding:"8px 12px", background:"var(--bg-hover)", borderRadius:"var(--radius-sm)", fontSize:"13px", color:"var(--text-2)" }` | `className="chip"` |
| 106 | `{ padding:"8px 16px 12px" }` | `className="section-pad"` |
| 108 | `{ color:"var(--text-3)", fontSize:"13px" }` | `className="text-meta"` |
| 110 | `{ display:"flex", flexDirection:"column", gap:"8px" }` | `className="flex-col-gap-sm"` |
| 112 | 同 88 | `className="chip"` |
| 130 | `{ padding:"8px 16px 12px" }` | `className="section-pad"` |
| 132 | `{ color:"var(--text-3)", fontSize:"13px" }` | `className="text-meta"` |
| 135 | `{ padding:"6px 0", borderBottom:"1px solid var(--border-light)" }` | `className="list-row"` |
| 137 | `{ fontSize:"13px", color:"var(--text-2)", marginTop:"2px" }` | `className="text-muted-mt"` |

- [ ] **Step 3: SettingsAgentPage 逐行迁移（19 处）**

| 行号 | 当前 `style={{}}` | 替换为 |
|------|-------------------|--------|
| 63 | `{ color:"var(--text-3)" }` | `className="text-muted"` |
| 94 | `{ display:"flex", alignItems:"center", gap:"16px" }` | 保持 inline（`// 一次性 gap:16px 容器`——全部文件仅此一处，无复用价值，不建类） |
| 103 | `{ fontSize:"14px", fontWeight:600, color:"var(--text-1)", minWidth:"24px" }` | `className="iter-value"` |
| 109 | `{ display:"flex", gap:"12px" }` | `className="flex-row-gap"` |
| 110 | `{ display:"flex", alignItems:"center", gap:"6px", cursor:"pointer", fontSize:"14px" }` | `className="radio-label"` |
| 119 | 同上 | `className="radio-label"` |
| 129 | `{ fontSize:"12px", color:"var(--text-3)", marginTop:"4px" }` | `className="text-hint-mt"` |
| 136 | `{ display:"flex", gap:"12px", marginBottom:"8px" }` | `className="flex-row-gap mb-sm"` |
| 137 | 同 110 | `className="radio-label"` |
| 146 | 同 110 | `className="radio-label"` |
| 167 | `{ fontSize:"13px", fontWeight:600, marginLeft:"8px" }` | `className="value-label"` |
| 170 | `{ fontSize:"12px", color:"var(--text-3)", marginTop:"4px" }` | `className="text-hint-mt"` |
| 186 | `{ fontSize:"12px", color:"var(--text-3)" }` | `className="text-hint"` |
| 200 | `{ fontSize:"12px", color:"var(--text-3)" }` | `className="text-hint"` |
| 205 | `{ display:"flex", gap:"12px" }` | `className="flex-row-gap"` |
| 206 | 同 110 | `className="radio-label"` |
| 216 | 同 110 | `className="radio-label"` |
| 226 | 同 110 | `className="radio-label"` |
| 237 | `{ fontSize:"12px", color:"var(--text-3)", marginTop:"4px" }` | `className="text-hint-mt"` |

- [ ] **Step 4: 前端门禁验证**

Run: `cd web && npm run build && npm run lint`

Expected: 无 TS 错误、`0 errors`。手工走查：技能管理/记忆管理/Agent 设置三页视觉与迁移前一致。

- [ ] **Step 5: Commit**

```bash
git add web/src/pages/SkillManagerPage.tsx web/src/pages/MemoryPage.tsx web/src/pages/SettingsAgentPage.tsx
git commit -m "refactor: consolidate skill/memory/agent-page inline styles into CSS classes"
```

---

## Task 11: C-2 批次 3 —— 剩余文件（约 60 处）

**Files:**
- Modify: `web/src/pages/SettingsExtensionsPage.tsx`、`web/src/pages/SettingsERPPage.tsx`、`web/src/pages/CronJobPage.tsx`、`web/src/pages/ToolsPage.tsx`、`web/src/components/Skeleton.tsx`、`web/src/pages/HistoryPage.tsx`、`web/src/components/Layout.tsx`、`web/src/pages/SettingsLLMPage.tsx`、`web/src/components/Sidebar.tsx`、`web/src/components/ChatMessage.tsx`、`web/src/components/ChatInput.tsx`

**Interfaces:**
- Consumes: Task 9 Step 1 工具类全集。
- Produces: 无新 CSS。
- **先后关系**：SettingsERPPage 的测试结果条（390-396 行）**不在本批次**，由 Task 12 (C-3) 统一处理，避免双重编辑；HistoryPage:78（hash 徽章）由 Task 13 (C-4) 处理；ToolsPage:94 已被 Task 8 替换为 `card-grid`，本表不含该行。

- [ ] **Step 1: SettingsExtensionsPage 逐行迁移（13 处）**

| 行号 | 当前 `style={{}}` | 替换为 |
|------|-------------------|--------|
| 85 | `{ marginBottom:"16px" }` | 追加 `mb-lg` |
| 90 | `{ display:"flex", alignItems:"center", gap:"12px", marginBottom:"20px", flexWrap:"wrap" }` | `className="toolbar"` |
| 93 | `{ maxWidth:"280px" }` | 保持 inline（一次性） |
| 98 | `{ fontSize:"13px", color:"var(--text-3)" }` | `className="text-meta"` |
| 105 | `{ marginLeft:"auto" }` | 保持 inline（一次性） |
| 114 | `{ opacity:0.3 }` | 追加 `empty-state-icon-dim` |
| 131-138 | 状态点（**动态颜色** `ext.enabled ? ... : ...`） | 保持 inline（`// dynamic: ext.enabled`） |
| 139 | `{ fontSize:"14px", fontWeight:600 }` | `className="text-name"` |
| 142-149 | KIND 徽章（**动态 background** `KIND_COLOR[ext.kind]` + `#fff` 白字） | 保持 inline（`// dynamic: KIND_COLOR`——spec C-3 注明白字类保持现状） |
| 157 | `{ height:"28px", width:"28px", padding:0, justifyContent:"center" }` | 追加 `icon-btn-sm` |
| 170 | `{ padding:"0 14px 14px", fontSize:"12px", color:"var(--text-3)", lineHeight:1.6 }` | `className="skill-card-desc-pad"` |
| 178 | `{ marginTop:"24px", fontSize:"12px", color:"var(--text-3)", lineHeight:1.6 }` | `className="footnote"` |
| 180 | `{ marginTop:"6px", paddingLeft:"20px" }` | `className="footnote-list"` |

- [ ] **Step 2: SettingsERPPage 逐行迁移（11 处，不含 390-396）**

| 行号 | 当前 `style={{}}` | 替换为 |
|------|-------------------|--------|
| 186 | `{ alignItems:"center" }` | 保持 inline（toast 微调一次性） |
| 194-199 | 段落 `{ margin:"0 0 20px", color:"var(--text-2)", lineHeight:1.6, fontSize:14 }` | `className="page-intro"` |
| 206-214 | tab 容器 `{ display:"inline-flex", background:"var(--bg-card)", border:"1px solid var(--border)", borderRadius:"var(--radius)", padding:4, marginBottom:20, gap:4 }` | `className="erp-tabs"` |
| 223-235 | tab 按钮（**动态** `isActive`） | 保持 inline（`// dynamic: isActive`） |
| 239-245 | tab 徽章（**动态** `isActive`） | 保持 inline（`// dynamic: isActive`） |
| 308 | `{ padding: 24 }` | `className="p-lg"` |
| 323-327 | card-header 冗余 inline（与 `.card-header` 类定义完全一致） | **直接删除该 `style={{...}}`**（`.card-header` 已有 display:flex/align-items/justify-content） |
| 331 | `{ margin:"4px 0 0", fontSize:12, color:"var(--text-3)" }` | `className="card-subtitle-sm"` |
| 339-347 | 启用/停用按钮（**动态** `enabled ? ... : ...`） | 保持 inline（`// dynamic: enabled`） |
| 373 | `{ padding:"8px 0", fontSize:14 }` | `className="field-value"` |
| 390-396 | 测试连接结果条 | **本批次跳过，Task 12 (C-3) 处理** |

- [ ] **Step 3: CronJobPage 逐行迁移（11 处）**

| 行号 | 当前 `style={{}}` | 替换为 |
|------|-------------------|--------|
| 159 | `{ marginBottom:12 }` | 追加 `mb-md` |
| 163 | `{ color:"var(--danger)", marginBottom:12 }` | `className="error-inline"` |
| 166 | `{ marginBottom:16, padding:16 }` | 追加 `mb-lg p-md` |
| 184 | `{ color:"var(--text-3)" }` | `className="text-muted"` |
| 198 | `{ display:"flex", gap:8 }` | `className="flex-row-gap-sm"` |
| 208 | `{ color:"var(--text-3)" }` | `className="text-muted"` |
| 226 | `{ padding:0 }` | 保持 inline（表格单元格一次性） |
| 227 | `{ margin:8, padding:16 }` | `className="job-edit-card"`（保持原视觉——spec C-1 的 margin 删除仅在有网格时适用，本页是表格无网格） |
| 243 | `{ display:"flex", gap:8 }` | `className="flex-row-gap-sm"` |
| 258 | 查看按钮复合样式 | `className="link-btn"` |
| 296 | `{ marginLeft:8, color:"var(--text-3)", fontSize:12 }` | `className="ml-sm text-hint"` |

- [ ] **Step 4: 剩余文件逐行迁移（共 25 处）**

`ToolsPage.tsx`（8 处，94 行已由 C-1 处理）：

| 行号 | 当前 `style={{}}` | 替换为 |
|------|-------------------|--------|
| 70 | `{ marginBottom:"16px", display:"flex", alignItems:"center", gap:"12px", flexWrap:"wrap" }` | `className="toolbar-md"` |
| 80 | `{ fontSize:"13px", color:"var(--text-3)" }` | `className="text-meta"` |
| 84 | `{ color:"var(--border)" }` | `className="text-sep"` |
| 90 | `{ opacity:0.3 }` | 追加 `empty-state-icon-dim` |
| 109 | `{ padding:"8px 0" }` | 保持 inline（一次性） |
| 114-120 | 状态点（静态 `background:"var(--success)"`） | `className="dot-success"` |
| 121 | `{ fontSize:"14px", fontWeight:600 }` | `className="text-name"` |

`Skeleton.tsx`（6 处，全部保持 inline——skeleton 一次性尺寸）：38/40/54（width 百分比）、51（`padding:"4px 0"`）、55（`marginTop:24`）、67（`height:140, borderRadius:"var(--radius-lg)"`）。

`HistoryPage.tsx`（3 处，78 行由 C-4 处理）：

| 行号 | 当前 `style={{}}` | 替换为 |
|------|-------------------|--------|
| 71 | `{ marginRight:"8px" }` | 追加 `mr-sm` |
| 74 | `{ marginTop:"4px" }` | 追加 `mt-sm` |

`Layout.tsx`（3 处）：

| 行号 | 当前 `style={{}}` | 替换为 |
|------|-------------------|--------|
| 29 | `{ fontSize:"12px", color:"var(--text-3)", marginLeft:"8px" }` | `className="text-hint ml-sm"` |
| 35 | `{ display:"flex", alignItems:"center", gap:"6px" }` | `className="flex-row-gap-6"` |
| 36 | `{ display:"inline-block" }` | 保持 inline（一次性） |

`SettingsLLMPage.tsx`（2 处）：

| 行号 | 当前 `style={{}}` | 替换为 |
|------|-------------------|--------|
| 169 | `{ marginTop:8, fontSize:13, color:"var(--text-2)" }` | 保持 inline（一次性模型信息） |
| 170 | `{ verticalAlign:"middle", marginRight:4 }` | 保持 inline（Icon 微调一次性） |

`Sidebar.tsx`（1 处）：41 行 `{ width:22, height:22, borderRadius:6 }` 保持 inline（一次性图片尺寸）。

`ChatMessage.tsx`（1 处）：84 行 `{ display:"inline-flex", gap:"12px" }` 保持 inline（一次性）。

`ChatInput.tsx`（1 处）：302 行 `{ display:"none" }` 保持 inline（file input 隐藏惯例）。

- [ ] **Step 5: 前端门禁验证 + 统计核对**

Run: `cd web && npm run build && npm run lint`

Expected: 无 TS 错误、`0 errors`。手工走查：扩展管理/ERP 连接/定时任务/内置工具/历史对话/骨架屏等页面视觉与迁移前一致。

核对收敛进度：

Run: `cd .. && rg -c "style=\{\{" web/src --count-matches -g "*.tsx" | awk -F: '{s+=$2} END {print s " inline styles remain"}'`

Expected: 剩余 inline 数量 ≈ 41（168 总 − Task 8 C-1 移除 2 − Task 9/10/11 迁移 125；其余均为"动态/一次性/保留"清单中的项，Task 12/13 还会再移除 SettingsERPPage:390 与 HistoryPage:78 两处）。

- [ ] **Step 6: Commit**

```bash
git add web/src/pages web/src/components
git commit -m "refactor: consolidate remaining page inline styles into CSS classes"
```

---

## Task 12: C-3 —— ERP 测试连接结果条 CSS 化

**Files:**
- Modify: `web/src/pages/SettingsERPPage.tsx:389-400`（测试结果条 JSX）
- Modify: `web/src/styles/global.css`（`.test-result-ok`/`.test-result-error` + form-grid-2 跨列规则）

**Interfaces:**
- Consumes: `testResult: { ok: boolean; message: string } | null`（SettingsERPPage TabProps）；`--success`/`--danger` 变量（`:root`）。
- Produces: 两个语义类 `.test-result-ok`/`.test-result-error`；`#B7EB8F`/`#FFA39E` 硬编码删除（spec 已拍板边框改用 `var(--success)`/`var(--danger)`，与背景/文字同系）；该处 inline style 顺带消除（C-2 交集）。
- **先后关系**：Task 11 批次 3 已明确跳过 390-396 行，本任务独占该 JSX 区块。

- [ ] **Step 1: global.css 新增结果条类**

在 Task 8 添加的 `.form-grid-2` 规则块之后追加：

```css
.form-grid-2 > .test-result-ok,
.form-grid-2 > .test-result-error {
  grid-column: 1 / -1;
}

.test-result-ok {
  margin-top: 12px;
  padding: 10px 14px;
  border-radius: var(--radius);
  font-size: 13px;
  display: flex;
  align-items: center;
  gap: 8px;
  background: var(--success-bg);
  color: var(--success);
  border: 1px solid var(--success);
}
.test-result-error {
  margin-top: 12px;
  padding: 10px 14px;
  border-radius: var(--radius);
  font-size: 13px;
  display: flex;
  align-items: center;
  gap: 8px;
  background: var(--danger-bg);
  color: var(--danger);
  border: 1px solid var(--danger);
}
```

- [ ] **Step 2: SettingsERPPage JSX 替换**

`web/src/pages/SettingsERPPage.tsx:389-400` 整段替换为：

```tsx
          {testResult && (
            <div className={testResult.ok ? "test-result-ok" : "test-result-error"}>
              {testResult.ok ? <IconCircleCheck size={14} /> : <IconAlertCircle size={14} />}
              {testResult.message}
            </div>
          )}
```

- [ ] **Step 3: 前端门禁验证**

Run: `cd web && npm run build && npm run lint`

Expected: 无 TS 错误、`0 errors`。手工走查（spec §5.2）：ERP 连接页点"测试连接"，成功/失败结果条配色正常（绿/红系）。

- [ ] **Step 4: Commit**

```bash
git add web/src/pages/SettingsERPPage.tsx web/src/styles/global.css
git commit -m "refactor: convert ERP test-result bar to semantic CSS classes"
```

---

## Task 13: C-4 —— 历史对话页 hash 徽章中性灰 + 删除二次确认

**Files:**
- Modify: `web/src/pages/HistoryPage.tsx:37-41,78`
- Modify: `web/src/styles/global.css`（`.badge-neutral`）

**Interfaces:**
- Consumes: `SessionSummary.id`（`web/src/types/index.ts`）；`handleDelete(sid)` 现有签名；`.badge` 基类（global.css:1298-1305）。
- Produces: `.badge-neutral`（`background: var(--bg-hover); color: var(--text-3); font-size: 12px;`——`font-size` 保留原 inline 的 12px 视觉）；`handleDelete` 首行 `window.confirm` 拦截。
- **先后关系**：HistoryPage:78 的 inline `fontSize:"12px"` 随本任务删除（Task 11 批次 3 已跳过该行）；HistoryPage:71/74 的 inline 已在 Task 11 处理。

- [ ] **Step 1: global.css 新增 `.badge-neutral`**

在 `.badge-inactive` 规则（1311 行）之后追加：

```css
.badge-neutral { background: var(--bg-hover); color: var(--text-3); font-size: 12px; }
```

- [ ] **Step 2: HistoryPage hash 徽章替换**

`web/src/pages/HistoryPage.tsx:78` 替换为：

```tsx
                <span className="badge badge-neutral">{s.id.substring(0, 6)}</span>
```

（保留 6 位 ID 便于排障；红色 `badge-primary` 语义移除。）

- [ ] **Step 3: 删除按钮二次确认**

`web/src/pages/HistoryPage.tsx:37-41` 的 `handleDelete` 首行插入确认：

```ts
  const handleDelete = async (sid: string) => {
    if (!window.confirm("确定删除该对话？删除后不可恢复。")) return;
    await api.del(`/sessions/${sid}`);
    setSessions((p) => p.filter((s) => s.id !== sid));
    if (state.currentSessionId === sid) dispatch({ type: "NEW_SESSION" });
  };
```

- [ ] **Step 4: 前端门禁验证**

Run: `cd web && npm run build && npm run lint`

Expected: 无 TS 错误、`0 errors`。手工走查（spec §5.2）：历史页会话卡 hash 为中性灰徽章；点"删除"弹出确认框，确认后删除、取消则不删。

- [ ] **Step 5: Commit**

```bash
git add web/src/pages/HistoryPage.tsx web/src/styles/global.css
git commit -m "feat: neutral hash badge and delete confirmation on history page"
```

---

# D 组 —— v1.9.0 发布（最后执行）

## Task 14: D —— 版本四件套 + 全量回归 + tag

**Files:**
- Modify: `pyproject.toml:7`、`package.json:3`、`CHANGELOG.md`（顶部）、`README.md:85-87`

**Interfaces:**
- Consumes: `AGENTS.md §10` 版本发布流程（pyproject 为唯一事实源）；上一会话内核重写摘要（`git log 1671a13^..HEAD` 中的 `1671a13`）；本计划 Task 1-13 全部改动。
- Produces: 版本 1.8.1 → 1.9.0 四处一致；`git tag v1.9.0` 打在发布 commit 并推送 origin（atomgit）。

- [ ] **Step 1: bump pyproject + package.json**

`pyproject.toml:7`：`version = "1.8.1"` → `version = "1.9.0"`

`package.json:3`：`"version": "1.8.1"` → `"version": "1.9.0"`

- [ ] **Step 2: CHANGELOG 新增 v1.9.0 章节**

在 `CHANGELOG.md` 顶部（`# Changelog` 之后、`## v1.8.1` 之前）插入：

```markdown
## v1.9.0 — 2026-08-06 (维护发布：内核重写收尾 + .env + 前端优化)

**范围**: 上一会话 Pi 风格内核重写（merge 1671a13）的收尾发布：6 个 parked minors 清理、TAVILY_API_KEY 运行时 .env 加载、前端宽屏布局与样式收敛。

### 改动

- **审批拒绝卡片显示"已拒绝"** (M-1): `ToolExecutionEnd` 新增可选字段 `denied`（默认 False）逐层透传——tool_dispatcher 的 denial 分支标记 → WS `tool_result.denied` → 前端 `_denied` → `ToolStepCard`"已拒绝"状态（warning 色系），替代原"执行失败"
- **取消持久化补全流式内容** (M-2): 取消分支把 `_stream_cb` 累积的 `_partial_response` 追加进 result messages，`final_response` 保持空——WS done 与前端 UI 逐字不变，仅影响持久化
- **桥工具卡片 args 回退** (M-3): `ToolStepCard.effectiveArgs` 回退链（pending 内容空白 → LLM 原始参数）+ `extractSubtitle` 补 `tool_search`(query)/`tool_describe`/`tool_call`(name) case
- **压缩 PhaseChange 补 session_id** (M-5): `_transform_context_hook` 两处 `_set_phase` 补 `session_id=self._session_id`
- **Agent 复用防御** (M-6): `run_async` 始终采纳显式新 token / 取消后自动重置新 token；`subscribe` 幂等化
- **删除同步 `dispatch_tool` 死代码** (M-7): `tool_dispatcher.py` 与 `agent/core/__init__.py` re-export 一并清理（grep 确认零引用）
- **TAVILY_API_KEY 运行时加载** (B): key 写入 `~/.zlink-agent/.env`（用户数据目录，**不打包**进 PyInstaller）；`backend/config.py` 改 `_load_env_files()` 候选列表加载（用户级 → 仓库根，`override=False` 环境变量优先）；打包版启动即生效
- **宽屏布局** (C-1): `.page-container` 1040px → 1280px；列表页 `.card-grid`（auto-fill 320px）+ 配置页 `.form-grid-2`（auto-fit 340px）——历史/记忆/工具页多列、LLM/Agent/ERP 设置两栏，窄屏自动回落
- **内联样式收敛** (C-2): 168 处 `style={{}}` 纯静态样式收敛为 37 个工具类（逐字复制属性，零视觉变化）；动态样式保留 inline 并注释
- **ERP 测试结果条 CSS 化** (C-3): `SettingsERPPage` 测试连接结果条收敛为 `.test-result-ok/error` 类（全 `var(--success)`/`var(--danger)` 系，删除 `#B7EB8F`/`#FFA39E` 硬编码）
- **历史页 hash 中性灰 + 删除确认** (C-4): 会话 ID 徽章 `badge-primary` → `badge-neutral`（保留 6 位 ID 排障用途）；删除按钮加 `window.confirm` 二次确认

### 上一会话内核重写（随本版本发布）

- **Pi 风格内核** (`agent/core/`): `kernel_types.py`（10 种 AgentEvent + AgentLoopConfig 钩子 + CancelToken）+ `loop.py`（零策略双层 async loop，AgentEnd 保证收尾）+ `agent.py`（有状态 Agent：subscribe/steer/cancel/wait_idle）+ `agent_adapter.py`（AIAgent 兼容层，四契约冻结）
- **并行工具执行**: prepare 串行 → gather 并发 → 保序；含 sequential 工具整批降级；25 个写工具标 `execution_mode="sequential"`（审计原则：拿不准一律 sequential）
- **截断防护**: `finish_reason→stop_reason` 映射补全（openai_compat/anthropic）；`stop_reason=="length"` 时截断消息的工具调用拒绝执行
- **CancelToken 全链路 + steering**: WS `{"type":"steering"}` 入站 + 前端生成中可发消息
- **web_search provider 降级链**: SEARCH_API_URL → Tavily → Brave → DDG 末档，错误分类降级、聚合报错
- **glob 根目录护栏** (`file_tools._is_root_scope`)、`start.sh` 不再自动开浏览器
```

- [ ] **Step 3: README 版本同步**

`README.md:85-87`（§版本）：

```markdown
## 版本

v1.9.0 — 2026-08-06
```

快速核对 README 的 feature list / structure 章节是否存在与 `AGENTS.md` 新内核（`agent/core/` 四文件 + 并行工具 + steering）矛盾的过时描述；如有，一并同步（保持与 AGENTS.md 一致即可，不做大规模改写）。

- [ ] **Step 4: 全量回归（spec §6 验收标准 1-4 全量核对）**

Run: `.venv/bin/python -m pytest tests/ -q`

Expected: `>= 501 passed`（501 + 本计划新增用例：Task 2 +1、Task 4 +1、Task 5 +3、Task 7 +2 ≈ 508），`0 failed`。

Run: `ruff check .`

Expected: 无输出（clean）。

Run: `cd web && npm run build && npm run lint && cd ..`

Expected: 无 TS 错误、`0 errors`。

核对四处版本一致：

Run: `rg -n "version" pyproject.toml package.json | head -2 && rg -n "v1\.9\.0" CHANGELOG.md README.md | head -3`

Expected: `pyproject.toml` 与 `package.json` 均为 `1.9.0`；CHANGELOG 顶部与 README §版本均为 `v1.9.0`。

核对四契约未被破坏（`tests/test_contract_freeze.py` 即 C1-C4 逐条断言）：

Run: `.venv/bin/python -m pytest tests/test_contract_freeze.py -v`

Expected: PASS，全部契约用例通过。

- [ ] **Step 5: 发布 commit + tag**

```bash
git add pyproject.toml package.json CHANGELOG.md README.md
git commit -m "release: v1.9.0 (parked minors cleanup, env loading, frontend polish)"
git tag v1.9.0
git push origin main
git push origin v1.9.0
```

Expected: 推送成功（remote 为 atomgit.com/gcw_cJbJuamU/zlink-agent.git）。验证 tag 打在发布 commit：

Run: `git log --oneline -3 && git tag -n1 v1.9.0`

Expected: 发布 commit `release: v1.9.0 ...` 在 `git log` 顶部；`v1.9.0` tag 指向该 commit；其下方可见 Task 1-13 的全部改动 commit。

- [ ] **Step 6: 全量验收（spec §6 逐项确认）**

- A 完成：`pytest tests/ -q` 全绿 + `ruff check .` 干净（Task 1-6 已逐任务验证）；M-1 审批拒绝卡显示"已拒绝"——dev 联调走查（`./start.sh --dev`，对话中触发高风险工具并拒绝审批，确认卡片 warning 色 + "已拒绝"文案）。
- B 完成：`~/.zlink-agent/.env` 含用户 key（Task 7 Step 5）；打包冒烟通过（Task 7 Step 6）；打包版 `web_search` 命中 Tavily（人工验证）。
- C 完成：Task 8-13 每批 build + lint 全绿；§5.2 走查项全部符合预期。
- D 完成：四处版本一致 1.9.0；`git tag v1.9.0` 已推送；`git log` 显示 A/B/C 改动全部在发布 commit 之前。
- 全程未破坏四契约（spec §6 验收标准 5；Task 14 Step 4 的 contract_freeze 全绿）。

---

## Self-Review 记录（plan 阶段内完成，实现前不再复查）

- **Spec 覆盖率**：spec §4 每个小节均有对应任务（A-1→T1、A-2→T2、A-3→T3、A-4→T4、A-5→T5、A-6→T6、B→T7、C-1→T8、C-2→T9/10/11、C-3→T12、C-4→T13、D→T14）；§5 测试计划（M-1/M-2/M-5/M-6/M-7/B/C 门禁）逐项落入任务；§6 验收标准由 T14 Step 6 汇总核对；§7 风险缓解（R1-R6）写入 Global Constraints 与相关任务（R1→T1 可选字段、R2→T7 override=False 不打包、R3→T2 final_response 不变、R4→T8 auto-fill 回落、R5→T9-11 机械迁移分批门禁、R6→T14 四处一致核对）；§8 决策摘要全文落入 Global Constraints。
- **已核实的事实修正（实现期以任务内行号为准）**：① CronJobPage 的 job 列表是 `<table className="data-table">` 而非卡片——`card-grid` 不适用（spec 列它为五个列表页之一，属 spec 依据记忆撰写；本计划按已核实结构处理，避免破坏表格布局）；② SkillManagerPage/SettingsExtensionsPage 已用 `.skill-grid`（auto-fill 340px），本就多列，仅受益于更宽容器；③ MemoryPage 的"卡片区"实为三个 `toolset-group` 手风琴，`card-grid` 套在其外层容器使其宽屏并排；④ SettingsExtensionsPage 无 `.card > .form-group` 结构，`form-grid-2` 不适用；⑤ `var(--bg-2)`/`var(--bg)`/`var(--error)` 在 CSS 中未定义（inline 里引用，计算行为为继承/无效）——C-2 类体逐字复制保留该行为，不"顺手修复"。
- **占位符扫描**：全文无 TBD/TODO/"参考 Task N"/"适当处理错误"；唯一 `<USER_PROVIDED_KEY>` 是 spec 明确的用户输入点（spec 不写真实值）。
- **类型/签名一致性**：`ToolExecutionEnd.denied`（kernel_types）→ `_HandlerResult.denied`（tool_dispatcher）→ WS `payload["denied"]`（chat.py）→ `denied?: boolean`（types）→ `_denied`（Message/AppContext）→ `ToolStepCard` 读取 `result?._denied`，链上字段名在各任务间逐字一致；`_partial_response` 在 T2 三处改动（__init__/重置/_stream_cb/取消分支）同名同义；M-6 的 `run_async`/`subscribe` 签名未变；`_load_env_files()` 在 T7 的测试与实现中签名一致。
